cd ../example07
docker-compose up -d

docker ps

docker exec example07_db_1 mysql -uroot -prootpwd example -e "

show tables;

select * from users;
"

docker exec example07_db_1 mysql -uroot -prootpwd example -e "
update users set lname = 'Williams' where id =2 ;
"

docker-compose down
docker-compose rm
docker volume ls

# see it's still there
docker-compose up -d


# don't use the volumes flag if you want to persist the database data
docker-compose down -v

